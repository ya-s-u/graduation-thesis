machine:
  timezone:
    Asia/Tokyo
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t ogajun/latex .

test:
  override:
    - cd ~/docker-latex
    - ./build.sh example ./tex_src
  post:
    - docker cp tex:/data/main.pdf $CIRCLE_ARTIFACTS/

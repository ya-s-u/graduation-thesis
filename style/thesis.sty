%% 章と見出しの間に改行\def\@makechapterhead#1{\hbox{}%  \vskip2\Cvs  {\parindent\z@   \raggedright   \reset@font\huge\bfseries   \ifnum \c@secnumdepth >\m@ne      \setlength\@tempdima{\linewidth}%      \setbox\z@\hbox{\@chapapp\thechapter\@chappos\hskip1zw}%      \unhbox\z@\nobreak      \par\vskip 20pt plus .5ex minus .5ex\vtop{\hsize\@tempdima#1}%   \fi}\vskip3\Cvs\newpage}%% 目次に項目を追加\def\addcontent#1{%  \addcontentsline{toc}{chapter}{#1}  \markboth{#1}{#1}}%% 章の最初のページに対する処理パッチ\def\ps@footnombre{\let\@mkboth\markboth  \def\@oddhead{\hfill\it\thepage}  \def\@evenhead{\hfill\it\thepage}%  \def\@evenhead{\thepage\it\hfill}  \def\@oddfoot{}  \def\@evenfoot{}}\def\chapter{\clearpage \thispagestyle{footnombre} \global\@topnum\z@\@afterindenttrue \secdef\@chapter\@schapter} %% Overfull \hboxを起こしにくくする\def\sloppy{\tolerance=9999 \hfuzz=.5\p@ \vfuzz=.5\p@}% description 環境での不具合を直す\def\descriptionlabel#1{\bf #1\hfil}\def\description{\list{}{%  \labelwidth=\leftmargin  \advance \labelwidth by -\labelsep  \let \makelabel=\descriptionlabel}}% ref での不具合を直す\let\refsave=\ref\def\ref#1{\mbox{\refsave{#1}}}\let\pagerefsave=\pageref\def\pageref#1{\mbox{\pagerefsave{#1}}}%% fancyhdrの設定\lhead[\it\thepage]{\bf\nouppercase{\leftmark}}\chead{}\rhead[\bf\nouppercase{\leftmark}]{\it\thepage}\lfoot[\fancyplain{\it\thepage}{{\bf\nouppercase{\rightmark}}}]{}\cfoot{}%\rfoot{\fancyplain{\it\thepage}{{\bf\nouppercase{\rightmark}}~~---~~\it\thepage}}\rfoot[]{\fancyplain{\it\thepage}{{\bf\nouppercase{\rightmark}}}}%% 引用記号の再定義%\def\@biblabel#1{[#1]}%\def\@cite#1#2{{$^{\mbox{\scriptsize $#1$}}$%%        \if@tempswa , #2\fi}$^{\mbox{\scriptsize $)$}}$ }\def\s{\mathop{\rm s}\nolimits}\def\tf{\mathop{\rm tf}\nolimits}\def\idf{\mathop{\rm idf}\nolimits}\def\similarity{\mathop{\rm similarity}\nolimits}\def\relation{\mathop{\rm relation}\nolimits}\def\distance{\mathop{\rm distance}\nolimits}%\def\argmax{\mathop{\rm argmax}\limits}   %%コメントアウト(2011/11/16)\def\sim{\mathop{\rm sim}\nolimits}\def\tm{\mathop{\rm tm}\nolimits}\def\minifigure{\def\@captype{figure}\medskip}\def\endminifigure{\def\@captype{}\medskip}\def\minitable{\def\@captype{table}\medskip}\let\endminitable=\endminifigure\def\minicapfigure#1{\vspace*{1ex}\hfil\refstepcounter{figure}%図\thefigure : #1\hfil\\}\def\minicaptable#1{\vspace*{1ex}\hfil\refstepcounter{table}%表\thetable : #1\hfil\\}